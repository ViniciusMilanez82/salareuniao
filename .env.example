# Frontend
VITE_API_URL=http://localhost:3001/api

# Backend (obrigatório em produção)
PORT=3001
NODE_ENV=development
JWT_SECRET=altere-em-producao-use-uma-string-longa-e-segura
JWT_EXPIRES_IN=7d

# Postgres (obrigatório em produção)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Criptografia de API keys em integration_settings (recomendado em produção)
# Use uma string de 32+ caracteres ou uma chave gerada (ex: openssl rand -base64 32)
# ENCRYPTION_KEY=

# Opcional: CORS em produção
# CORS_ORIGIN=https://seusite.com
